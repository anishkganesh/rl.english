# Backend URL (without protocol - wss:// is added automatically for non-localhost)
# For local development, leave empty or use localhost:8000
# For production, use your deployed backend URL (e.g., your-app.railway.app)
VITE_BACKEND_URL=localhost:8000

